const inquirer = require("inquirer");
const fs = require('fs');
const util = require("./utils/generateMarkdown.js");
const axios = require("axios");

// prompt users to answer questions regarding the README

// generate the view with the answers from the user in a return

// call function that prompted the questions to console.log that it was succesfully written